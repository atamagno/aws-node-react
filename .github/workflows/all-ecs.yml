name: Deploy All with ECS

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: Choose development environment to deploy
        options:
          - dev
          - test
          - prod
        default: dev
      region:
        type: string
        description: Choose AWS region
        default: ap-southeast-2
      deploy_ecr:
        type: boolean
        description: Deploy new ECR image?
        default: false
      task_count:
        type: number
        description: Number of ECS tasks to run
        default: 1
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      region:
        required: true
        type: string
      deploy_ecr:
        required: true
        type: boolean
      task_count:
        required: true
        type: number

permissions:
  id-token: write
jobs:
  deploy-common:
    uses: ./.github/workflows/infra-common.yml
    secrets: inherit
    with:
      environment: ${{ github.event.inputs.environment }}
      region: ${{ github.event.inputs.region }}

  deploy-infra:
    uses: ./.github/workflows/infra-ecs.yml
    needs: deploy-common
    secrets: inherit
    with:
      environment: ${{ github.event.inputs.environment }}
      region: ${{ github.event.inputs.region }}
      deploy_ecr: ${{ github.event.inputs.deploy_ecr == 'true' }}
      task_count: ${{ github.event.inputs.task_count }}

  deploy-frontend:
    uses: ./.github/workflows/frontend.yml
    needs: deploy-infra
    secrets: inherit
    with:
      environment: ${{ github.event.inputs.environment }}
      region: ${{ github.event.inputs.region }}
      backend: ecs
